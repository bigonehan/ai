# Input

## 2026-07-30

### Codex 사용자 요구 기록 규칙

- 요청: `~/ai/` 아래에서 관리하는 Codex 전역설정에, 프로젝트 변경 요구를 그날의 날짜별로 프로젝트 루트 `Input.md`에 정리해 저장하는 규칙을 추가한다.
- 완료 조건: 관리 원본과 실사용 전역 규칙을 함께 갱신하고, 새 규칙이 정상적으로 로드되는지 확인한다.

## 2026-08-15

### Test Manager 반복 오판 방지

- 요청: 동일 장애를 반복해서 해결했다고 잘못 보고한 세션 로그를 분석하고, 로그 없는 추측 수정, 실제 런타임 미검증 완료 주장, Grist 요청 중 Side Prompt 같은 인접 프로젝트 수정이 재발하지 않도록 Test Manager를 구체적으로 강화한다.
- 완료 조건: 반복 보고 hard stop, 로그 우선 진단, 변경 범위 격리, validator 직접 실행 증거, 실제 consumer 검증을 강제하고 허위 증거와 인접 범위 변경을 거부하는 회귀 검사를 통과한다.
- 추가 요청: 테스트할 때 unit test 통과와 실제 제품 동작 확인을 순서대로 수행하고, 두 결과를 같은 시나리오로 연결하는 필수 절차를 Test Manager에 추가한다.

## 2026-08-16

### Test Manager 요구 누락 및 런타임 오판 방지

- 요청: 테스트할 때 unit test 통과와 실제 제품 동작 확인을 순서대로 수행하고, 두 결과를 같은 시나리오로 연결하는 필수 절차를 Test Manager에 추가한다.
- 요청: Side Prompt Gallery 자동응답 다운로드 요구가 반복됐는데도 일부 대상만 계약에 넣고 localhost·Headless·오래된 회전 로그를 근거로 잘못 판정한 과정을 분석해 Test Manager를 수정한다.
- 완료 조건: 모든 요구·사이트별 scenario coverage, 회전 로그 신선도, 실제 Chrome 등록 경로·버전, action 이후 실제 output을 검증기가 강제하고 일부 대상이나 오래된 증거를 거부한다.
